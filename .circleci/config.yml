version: 2.1

orbs:
  clair: ovotech/clair-scanner@dev:report-path-fix

jobs:
  scan_image:
    executor: clair/default
    steps:
    - clair/scan:
        image: jenkins/jenkins:lts-slim

workflows:
  test:
    jobs:
    - scan_image
